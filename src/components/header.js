const Header = {
  render: () => {
    return `
      <nav>
        <a href="/#/">HOME</a>
        <a href="/#/signin">Login</a>
        <a href="#">Products</a>
      </nav>   
    `
  }
}

export default Header;